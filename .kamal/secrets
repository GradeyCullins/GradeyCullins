# Secrets defined here are available for reference under registry/password, env/secret, builder/secrets,
# and accessories/*/env/secret in config/deploy.yml. All secrets should be pulled from either
# password manager, ENV, or a file. DO NOT ENTER RAW CREDENTIALS HERE! This file needs to be safe for git.

#KAMAL_REGISTRY_PASSWORD=$(cat .env.local | grep KAMAL_REGISTRY_PASSWORD | cut -d '=' -f 2 | sed 's/^"//; s/"$//')
KAMAL_REGISTRY_PASSWORD=$(bin/rails credentials:fetch KAMAL_REGISTRY_PASSWORD)

#AWS_ACCESS_KEY_ID=$(cat .env.local | grep AWS_ACCESS_KEY_ID | cut -d '=' -f 2 | sed 's/^"//; s/"$//')
AWS_ACCESS_KEY_ID=$(bin/rails credentials:fetch AWS_ACCESS_KEY_ID)

#AWS_SECRET_ACCESS_KEY=$(cat .env.local | grep AWS_SECRET_ACCESS_KEY | cut -d '=' -f 2 | sed 's/^"//; s/"$//')
AWS_SECRET_ACCESS_KEY=$(bin/rails credentials:fetch AWS_SECRET_ACCESS_KEY)

#OPENAI_API_KEY=$(cat .env.local | grep OPENAI_API_KEY | cut -d '=' -f 2 | sed 's/^"//; s/"$//')
OPENAI_API_KEY=$(bin/rails credentials:fetch OPENAI_API_KEY)

RAILS_MASTER_KEY=$(cat config/master.key)
